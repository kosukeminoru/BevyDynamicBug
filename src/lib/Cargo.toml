[package]
name = "lib"
version = "0.1.0"
edition = "2021"

[lib]
name = "bevylib"
crate_type = ["dylib"]
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = {version = "0.7.0", feature = "dynamic"}